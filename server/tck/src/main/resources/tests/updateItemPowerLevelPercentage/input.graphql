# Tests that the implementation can handle a transformation on a primitive scalar (int).
mutation alterIronManSuitPower {
  updateItemPowerLevelPercentage(itemID:1001,powerLevel:"80%") {
    id
    name
    powerLevel
  }
}
